type Mutation {
  createSession(email: String!, password: String!): UserInfo
  updateSession: UpdateSession
  deleteSession: DeleteData
  deleteAllSession(currentPassword: String!): DeleteData
}

type Token {
  accessToken: String!
}

union UpdateSession = ErrorResponse | Token
