type Mutation {
  createSession(email: String!, password: String!): CreateSessionResponse!
}

union CreateSessionResponse = CreateSessionCredentialError | Token

type CreateSessionCredentialError implements Error {
  message: String!
}
