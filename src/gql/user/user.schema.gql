type Query {
  readUser: User
}

type Mutation {
  createUser(name: String, email: String, password: String): CreateUserResponse
  deleteUser(currentPassword: String): SuccessResponse
  updateUser(
    toUpdate: String
    email: String
    password: String
    name: String
    currentPassword: String
  ): User
}

type User {
  name: String
  email: String
}

type ErrorResponse {
  title: String
  message: String
}

type SuccessResponse {
  message: String
}

union CreateUserResponse = User | ErrorResponse
