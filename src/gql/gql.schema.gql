enum UserType {
  SELLER
  BUYER
}

type User {
  name: String!
  email: String!
  type: UserType!
}

type ErrorResponse {
  title: String!
  message: String!
}

type SuccessResponse {
  message: String!
}

union UserInfo = User | ErrorResponse
union DeleteData = SuccessResponse | ErrorResponse
