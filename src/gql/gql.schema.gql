type User {
  name: String!
  email: String!
}

type ErrorResponse {
  title: String!
  message: String!
  statusCode: Int!
}

type SuccessResponse {
  message: String!
}

union UserInfo = User | ErrorResponse
union DeleteData = SuccessResponse | ErrorResponse
