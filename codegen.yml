overwrite: true
schema: "src/gql/typeDefs/*.gql"
documents: null
config:
  allowEnumStringTypes: true
generates:
  src/types/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
